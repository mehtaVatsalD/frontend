import { RenderThisDirective } from './render-this.directive';

describe('RenderThisDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderThisDirective();
    expect(directive).toBeTruthy();
  });
});
