import { ExpenseemitbgchangerDirective } from './expenseemitbgchanger.directive';

describe('ExpenseemitbgchangerDirective', () => {
  it('should create an instance', () => {
    const directive = new ExpenseemitbgchangerDirective();
    expect(directive).toBeTruthy();
  });
});
