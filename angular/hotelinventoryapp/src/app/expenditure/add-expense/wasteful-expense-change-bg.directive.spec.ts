import { WastefulExpenseChangeBgDirective } from './wasteful-expense-change-bg.directive';

describe('WastefulExpenseChangeBgDirective', () => {
  it('should create an instance', () => {
    const directive = new WastefulExpenseChangeBgDirective();
    expect(directive).toBeTruthy();
  });
});
